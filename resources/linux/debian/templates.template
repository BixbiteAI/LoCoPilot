Template: @@NAME@@/add-microsoft-repo
Type: boolean
Default: true
Description: Add apt repository for LoCoPilot updates?
 The installer can add the package repository and signing key to update
 LoCoPilot through apt (when your distribution uses this update channel).
